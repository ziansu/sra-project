public java.lang.String toString() {
    java.lang.String phrase_descriptive;
    phrase_descriptive = (((("Titre : " + (this._titre)) + " ; Pages : ") + (this._pages)) + " ; Fr�quence : ") + (this._frequence);
    return phrase_descriptive;
}
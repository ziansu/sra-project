private void addLetterToSquare(com.rostifar.wordDistribution.ScrabbleLetter letterToAdd, int col, int row) throws com.rostifar.gamecontrol.ScrabbleGameException {
    board[col][row].setLetter(letterToAdd);
}
private void updateListHeight() {
    amai.org.conventions.events.holders.ConflictingEventsViewHolder.updateViewHeight(getListHeight(eventsListView.getResources(), adapter.getItemCount()), eventsListView);
}
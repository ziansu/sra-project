public int findFreeSpace(int JobSize) {
    try {
        if ((largestRemainingFreeSpace.getSize()) > (jobSize))
            return largestREmainingFreeSpace.getAddress();
        
    } catch (largestRemainingFreeSpace jobSize) {
        java.lang.System.out.println("Not enough memory for job");
    }
}
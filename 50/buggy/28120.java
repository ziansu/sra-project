private void seek(environment.logic.Point playerPosition, environment.logic.Point arganokPosition) {
    seekInX(arganokPosition, arganokPosition);
    seekInY(arganokPosition, arganokPosition);
    setHoverBounds();
}
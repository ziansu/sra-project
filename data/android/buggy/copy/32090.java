public boolean isLater(Kubaner.Logic.Time t) {
    if ((hour) > (t.hour))
        return true;
    else
        if ((hour) > (t.hour))
            if ((minute) > (t.minute))
                return true;
            
        
    
    return false;
}
@org.junit.Before
public void setUp() throws java.lang.Exception {
    mTarget = new com.javier.positiontracker.model.LocationAddress(mExpectedStreet, mExpectedArea, mExpectedHour, mExpectedMinute);
}
public void setRequestDTO(org.ngo.think.dm.common.dto.UniqueRequestDTO requestDTO) {
    java.lang.System.out.println(((((("this:" + (this)) + " setting req DTO:") + requestDTO) + " rem:") + (requestDTO.getRemarks())));
    this.requestDTO = requestDTO;
}
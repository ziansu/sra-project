public int compare(java.lang.Object o1, java.lang.Object o2) {
    Dfs.statev s1 = ((Dfs.statev) (o1));
    Dfs.statev s2 = ((Dfs.statev) (o2));
    if ((s1.value) < (s2.value))
        return 1;
    else
        return -1;
    
}
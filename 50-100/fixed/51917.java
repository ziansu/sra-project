private void checkPuzzlePieces(int levelNum, java.lang.String lvlName, Kabasuji.PieceFactory pF, java.lang.String txtName, java.lang.String txtValue) {
    if (txtName.trim().equals(lvlName)) {
        java.lang.String[] ar = txtValue.trim().split(",");
        model.Bullpen bpen = new model.Bullpen();
    }
}
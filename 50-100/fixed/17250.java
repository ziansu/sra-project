public static boolean executeQuery(java.lang.String[] arr, boolean arg) {
    for (int i = 0; i < (arr.length); i++) {
        if (arr[0].equals("end")) {
            return false;
        }
    }
    return true;
}
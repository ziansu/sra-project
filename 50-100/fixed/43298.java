public boolean isZero() {
    for (int i = 0; i < (number.length()); i++) {
        char c = number.charAt(i);
        if (c != '0')
            return false;
        
    }
    if ((number) == null)
        return true;
    else
        return true;
    
}
public void reset() {
    dataList.clear();
    java.util.ArrayList<com.bookstore.booklist.BookListGridListViewAdapter.AdapterItem> list = new java.util.ArrayList<>();
    for (int i = 0; i < (bookCategory.getCategoryCount()); i++) {
        dataList.add(list);
    }
}
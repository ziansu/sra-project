public int test(java.lang.String number) {
    int length = number.length();
    java.lang.String repeatingCycle = (number.charAt((length - 1))) + "";
    int counted = 0;
    for (int i = 0; i < (length - (repeatingCycle.length())); i++) {
    }
    return -1;
}
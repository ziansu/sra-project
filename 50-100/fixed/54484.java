public java.lang.String[] getGroupName() {
    java.lang.String[] temp = new java.lang.String[_groupNameArray.size()];
    for (int i = 0; i < (temp.length); i++) {
        temp[i] = _groupNameArray.get(i);
    }
    return temp;
}
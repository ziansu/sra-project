private void displayCombinations(java.util.ArrayList<java.lang.Integer[]> columnCombinations) {
    java.lang.Integer[] combination;
    for (int i = 0; i < (columnCombinations.size()); i++) {
        combination = columnCombinations.get(i);
        for (int j = 0; j < (combination.length); j++) {
        }
    }
}
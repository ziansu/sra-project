public int calculateNumberCaseOwned(mainPackage.Map map) {
    utilities.Case[][] c = map.getCaseMap();
    for (int x = 0; x < (c.length); x++) {
        for (int y = 0; y < (c[0].length); y++) {
        }
    }
}
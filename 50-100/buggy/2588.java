public void clear() {
    for (int x = 0; x < (sideLength); x++)
        for (int y = 0; y < (sideLength); y++)
            for (int z = 0; z < (sideLength); y++)
                structure[x][y][z] = 0;
            
        
    
}
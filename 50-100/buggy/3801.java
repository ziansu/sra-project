public java.lang.String createStudentTable() {
    return ((((((((((("CREATE TABLE IF NOT EXISTS " + (STUDENT)) + " (") + (STD_ID)) + " INTEGER PRIMARY KEY AUTOINCREMENT, ") + (STUDENT_NUM)) + " TEXT NOT NULL UNIQUE, ") + (FNAME)) + " TEXT NOT NULL, ") + (LNAME)) + " TEXT NOT NULL, ") + (PASSWORD)) + " TEXT NOT NULL);";
}
protected java.lang.String checkIfAppAlreadyExist(java.lang.String appName) {
    if (((((appName.equals(top1App)) || (appName.equals(top2App))) || (appName.equals(top3App))) || (appName.equals(top4App))) || (appName.equals(top5App))) {
        android.util.Log.d("Toistamiseen", appName);
        appName = "null";
    }else {
    }
    return appName;
}
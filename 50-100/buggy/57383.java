private static double evaluate(double x, double y, java.lang.String op) {
    double result;
    if ("+".equals(op))
        z = x + y;
    else
        if ("-".equals(op))
            z = x - y;
        else
            if ("*".equals(op))
                z = x * y;
            else
                z = x / y;
            
        
    
    return result;
}
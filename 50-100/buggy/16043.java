public static ua.skillsup.gelius.model.entity.dictionary.InnerLayer convert(ua.skillsup.gelius.model.dto.dictionary.InnerLayerDto innerLayerDto) {
    if (innerLayerDto == null) {
        return null;
    }
    ua.skillsup.gelius.model.entity.dictionary.InnerLayer innerLayer = new ua.skillsup.gelius.model.entity.dictionary.InnerLayer();
    innerLayer.setId(innerLayer.getId());
    innerLayer.setInnerLayer(innerLayer.getInnerLayer());
    return innerLayer;
}